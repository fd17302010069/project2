let recharge=document.getElementById("recharge_form");
let rechargeValue=document.getElementById("recharge_value").value;

recharge.onsubmit=function () {
    let rechargeAmount=parseInt(rechargeValue);
    //TODO: 充值金额为正整数
};